= form_for [@album, @video] do |f|
  - if @video.errors.any?
    #error_explanation
      %h2= "#{pluralize(@video.errors.count, "error")} prohibited this video from being saved:"
      %ul
        - @video.errors.full_messages.each do |message|
          %li= message

  .mb-3
    = f.label :original_file, class: 'form-label'
    = f.file_field :original_file, class: 'form-control'
  - if action_name == 'edit'
    .mb-3
      = f.label :created_at, class: 'form-label'
      = f.text_field :created_at, class: 'form-control', type: 'datetime-local', value: @video.created_at.strftime('%Y-%m-%dT%H:%M')
  .mb-3
    = f.label :description, class: 'form-label'
    = f.text_field :description, class: 'form-control'
  .actions
    = f.submit 'Speichern', class: 'btn btn-primary'
