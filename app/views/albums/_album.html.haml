.col-md-4.mb-3
  .card{id: "#{dom_id album}"}
    - if album.pictures.any?
      = link_to album do
        %img.card-img-top{ src: url_for(album.pictures.first.file.variant(:thumb)), alt: album.name }
    - else
      = link_to "Öffnen", album, class: 'card-link'
    .card-body
      %p= album.name
